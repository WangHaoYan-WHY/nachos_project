#include "syscall.h"

int
main()
{
  /*
  int child_id;
  child_id = Fork_P(1);
  ThreadJoin(child_id)
  child_id = Fork_P(2);
  ThreadJoin(child_id)
  child_id = Fork_P(3);
  ThreadJoin(child_id);
  */
  Exit(0);
}
